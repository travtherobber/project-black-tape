This directory is intentionally kept free of real user exports.

Rules:
- do not commit raw account archives here
- do not commit extracted HTML, JSON, images, or screenshots from personal exports
- tests generate synthetic fixture archives in memory instead of relying on committed personal data

If you need to inspect a private export locally, keep it outside the repo or in a gitignored path.
